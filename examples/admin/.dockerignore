# Generated by `fitz docker init` (Phase 12.4).
# Files that must NOT enter the Dockerfile build context.

# Compiler outputs
target/
.fitz/
fitz.lock

# Git + local configs
.git/
.gitignore
.github/

# Local secrets / env
.env
.env.*
!.env.example

# Editor + OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docker itself (no need to be in the build context)
Dockerfile
docker-compose.yml
.dockerignore

# Python (if the program uses interop)
__pycache__/
*.pyc
.venv/
venv/

# Boilerplate misc
node_modules/
